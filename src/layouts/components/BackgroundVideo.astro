---
const path = Astro.props;
---
<video
  autoplay
  muted
  playsinline
  class="fixed inset-0 -z-50 opacity-50 w-full h-full object-cover"
>
  <source src={path.src} type="video/mp4" />
  Your browser does not support the video tag.
</video>
