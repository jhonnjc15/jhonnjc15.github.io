---
const path = Astro.props;
---
<video
  autoplay
  muted
  playsinline
  class="fixed inset-0 -z-50 h-screen w-screen object-cover opacity-60"
>
  <source src={path.src} type="video/mp4" />
  Your browser does not support the video tag.
</video>
