---
const path = Astro.props;
---
<video
  autoplay
  muted
  playsinline
  class="absolute inset-0 -z-50 h-full w-full object-cover opacity-60"
>
  <source src={path.src} type="video/mp4" />
  Your browser does not support the video tag.
</video>
