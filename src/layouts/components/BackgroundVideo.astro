---
const path = Astro.props;
---
<video
    autoplay
    muted
    class= "absolute -z-50 opacity-50 inset-0 w-full h-full object-cover object-top"
  >
    <source
      src={path.src}
      type="video/mp4"
    />
    Your browser does not support the video tag.
  </video>
