---
import config from "@config/config.json";

const {
  call_to_action: {
    title,
    description,
    image,
    button: { label, link },
  },
} = config;
---

<section class="section px-5 py-0 xl:py-14">
  <div class="container">
    <div
      class="bg-gradient
            grid grid-cols-1 lg:grid-cols-2
            items-center gap-6 lg:gap-10
            rounded-[40px] px-4 py-7 lg:px-8 lg:py-10"
    >
      <div class="flex justify-center lg:justify-start">
        <div class="space-y-4 text-center lg:text-left max-w-xl">
          <h2 class="text-3xl md:text-4xl font-bold text-white leading-tight">
            {title}
          </h2>

          <p class="text-white text-base md:text-lg leading-relaxed">
            {description}
          </p>

          <a
            class="btn btn-white inline-flex w-auto items-center justify-center lg:justify-start"
            href={link}
            target="_blank"
            rel="noreferrer"
          >
            {label}
          </a>
        </div>
      </div>
            <div class="flex justify-center">
        {image && (
          <div class="overflow-hidden rounded-[24px] border border-white/20 bg-white/10 p-3 shadow-2xl backdrop-blur max-w-lg">
            <img
              src={image}
              alt="Divulgación científica 2025"
              class="h-full w-full rounded-2xl object-cover"
              loading="lazy"
            />
          </div>
        )}
      </div>
    </div>
  </div>
</section>
